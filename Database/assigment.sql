create database database1;

use database1;

create table student(
roll int primary key,
name varchar(50),
contact varchar(15),
city varchar(50),
email varchar(50),
standard varchar(50)
);

select * from student;

select * from User;
